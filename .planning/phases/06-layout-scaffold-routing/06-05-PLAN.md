---
phase: 06-layout-scaffold-routing
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - src/App.tsx
  - src/components/ReadingPaneStub.tsx
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04]
must_haves:
  truths:
    - "Switching from article A (scrolled down) to article B shows article B from the top, not mid-page"
    - "When no article is selected the reading pane shows the terminal placeholder '> select an article to read'"
    - "Selecting a row renders that article's sanitized Markdown body via the existing ArticleBody (rehype-sanitize intact, no <script> survives)"
    - "When the active filter hides the currently-selected article, the reading pane shows a 'hidden by current filter' notice with a control that restores the view"
  artifacts:
    - path: "src/components/ReadingPaneStub.tsx"
      provides: "Reading pane with scroll-reset key, hidden-by-filter notice + clear control, plus the existing placeholder/loading/404/body states"
      contains: "hidden by current filter"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/ReadingPaneStub.tsx"
      via: "passes selectedInFilteredList flag + onClearFilters; keys the scroll container on selectedNaddr"
      pattern: "ReadingPaneStub"
    - from: "src/components/ReadingPaneStub.tsx"
      to: "src/components/ArticleBody.tsx"
      via: "renders <ArticleBody content={article.content} /> unchanged (rehype-sanitize boundary)"
      pattern: "ArticleBody"
---

<objective>
Polish the reading pane: reset scroll-to-top on article switch (READ-03) and add a "hidden by current filter" notice with a restore control when the active hashtag/ML filter excludes the currently-selected article (READ-04). Confirm READ-01 (sanitized Markdown body via the unchanged `ArticleBody`) and READ-02 (terminal placeholder) still hold.

Purpose: The reading pane already renders the body (shipped in 06-03), but two correctness gaps remain — scroll carries over between articles, and a filter that hides the open article currently leaves the pane silently showing stale-but-correct content with no signal that the list and pane have diverged (Pitfall 3 / Pitfall 10).
Output: A scroll-reset keyed scroll container in `App.tsx` and a hidden-by-filter branch in `ReadingPaneStub` with a clear-filters control.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-layout-scaffold-routing/06-CONTEXT.md
@.planning/phases/06-layout-scaffold-routing/06-PATTERNS.md
@.planning/research/PITFALLS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scroll-reset the reading pane on article switch (READ-03)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (lines 214-224 — the right ResizablePanel and its inner `flex-1 overflow-y-auto h-full` scroll wrapper that mounts ReadingPaneStub)
    - .planning/research/PITFALLS.md (Pitfall 10 — `key` on the scroll container resets scrollTop on selection change)
  </read_first>
  <action>
    On the reading pane's inner scroll wrapper `<div className="flex-1 overflow-y-auto h-full">` (App.tsx line 216), add `key={selectedNaddr}`. When `selectedNaddr` changes, React remounts the scroll container, naturally resetting `scrollTop` to 0 (Pitfall 10 — the `key` approach, not an imperative ref). This is the only change in this task. Do NOT alter the memo chain, the panel sizes, or any other state.

    Edge case to preserve: when `selectedNaddr` is empty (`''`), the key is still valid (empty string is a stable key) — the placeholder state continues to render correctly. Do not key on `selectedArticle?.id` (it is null during cold-load/placeholder/404 and would collapse those distinct states onto one key).
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && grep -n "key={selectedNaddr}" src/App.tsx && npx tsc -b --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "key={selectedNaddr}" src/App.tsx` is exactly 1 and it is on the reading-pane scroll wrapper div.
    - `grep -c "key={selectedArticle" src/App.tsx` is 0 (did not key on the article id).
    - The frozen memo chain is unchanged: `grep -c "useMemo" src/App.tsx` equals its pre-change count (no added/removed memos).
    - `npx tsc -b --noEmit` reports no errors.
  </acceptance_criteria>
  <done>Selecting a new article remounts the reading-pane scroll container, resetting scroll to the top.</done>
</task>

<task type="auto">
  <name>Task 2: Hidden-by-filter notice with restore control (READ-04)</name>
  <files>src/App.tsx, src/components/ReadingPaneStub.tsx</files>
  <read_first>
    - src/App.tsx (lines 103-108 selectedArticle memo — filteredArticles-first then sortedArticles fallback; lines 94-97 filteredArticles; lines 117-123 onTagToggle; lines 199 the existing `setSelectedTags(new Set())` clear-filters pattern; lines 215-224 reading pane mount)
    - src/components/ReadingPaneStub.tsx (full file — the state ladder: no-selection / cold-load / 404 / article-present)
    - .planning/research/PITFALLS.md (Pitfall 3 — derive a "hidden" signal from filteredArticles membership; show a notice, do NOT auto-clear selection)
    - src/lib/nostr.ts (articleNaddr — to test filteredArticles membership)
  </read_first>
  <action>
    In `App.tsx`, derive a boolean that is true when the selected article exists but is NOT in the filtered list — i.e. it resolved only via the `sortedArticles` fallback (Pitfall 3). Compute it next to the `selectedArticle` memo without altering that memo: e.g. `const selectedHiddenByFilter = !!selectedArticle && !filteredArticles.some(a => articleNaddr(a) === selectedNaddr)`. This is a derived const (not new state, not a memo-chain edit) — v1.2 presentation-only.

    Pass two new props to `<ReadingPaneStub>`: `hiddenByFilter={selectedHiddenByFilter}` and `onClearFilters={() => setSelectedTags(new Set())}` (reuse the exact clear pattern already used by the `[FILTER]` empty-list button at App.tsx:199 — clearing hashtag tags restores the article to the filtered list; this is the simplest restore that satisfies READ-04's "way to clear the filter / restore the view"). Note in a code comment that ML-filtering can also hide an article, but the user-facing restore here clears hashtag tags — the ML toggle remains in the sidebar header and is the control for that path; clearing tags is the in-pane affordance.

    In `ReadingPaneStub`, add `hiddenByFilter: boolean` and `onClearFilters: () => void` to the props interface. Add a new branch BEFORE the article-present render: when `article` is present AND `hiddenByFilter` is true, render a terminal-styled notice (match the bracketed-tag convention from App.tsx `[ERR]`/`[EMPTY]`/`[FILTER]`): a `[FILTER]` line reading `[FILTER] this article is hidden by the current filter` plus a button styled like the existing clear-filters button (`crt-glow border border-terminal-border text-terminal-green font-mono text-xs px-4 py-2 hover:bg-terminal-surface`) labeled `> clear filters` that calls `onClearFilters`. Center it like the other pane states. Do NOT auto-clear the selection or mutate the hash (Pitfall 3 — the user may want to undo the filter and return to the article).

    Confirm (no change needed, but verify in the read) that the article-present branch still renders `<ArticleBody content={article.content} />` unchanged — READ-01 and the rehype-sanitize boundary (Pitfall 8) are preserved. Do not modify ArticleBody.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && npx tsc -b --noEmit 2>&1 | head -15 && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hiddenByFilter" src/components/ReadingPaneStub.tsx` is at least 2 (prop type + branch); `grep -c "hiddenByFilter" src/App.tsx` is at least 1.
    - `grep -c "this article is hidden by the current filter" src/components/ReadingPaneStub.tsx` is 1.
    - `grep -c "onClearFilters" src/components/ReadingPaneStub.tsx` is at least 1 and the button invokes it.
    - `grep -c "ArticleBody" src/components/ReadingPaneStub.tsx` is at least 1 (body render preserved); ArticleBody.tsx is untouched (`git diff --name-only` does not list src/components/ArticleBody.tsx).
    - No auto-clear: `grep -c "setSelectedNaddr" src/components/ReadingPaneStub.tsx` is 0 (the pane never mutates selection).
    - `npx tsc -b --noEmit` clean and `npx vitest run` passes.
  </acceptance_criteria>
  <done>When the active filter hides the open article, the pane shows a [FILTER] notice with a working clear-filters control; the body render path and ArticleBody remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nostr relay → app (`article.content`) | Author-controlled Markdown rendered in the reading pane — the XSS surface, mediated by ArticleBody's rehype-sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Elevation of Privilege | Reading-pane Markdown body (stored XSS) | mitigate | ArticleBody is reused unchanged; this plan adds no `<Markdown>` call and never introduces `dangerouslySetInnerHTML` or `rehype-raw` (Pitfall 8). Acceptance asserts ArticleBody.tsx is untouched and the body still routes through it |
| T-06-05-02 | Spoofing | Hidden-by-filter notice (pane/list divergence misleads the reader) | mitigate | The notice explicitly tells the user the article is filtered and offers a restore control rather than silently showing content the list no longer reflects (Pitfall 3) |
</threat_model>

<verification>
- `npx vitest run` passes (no data-layer regressions).
- `npx tsc -b --noEmit` clean.
- `npx vite build` succeeds.
- Manual dev sanity (formalized in 06-07): scroll article A down, click article B → B at top; select an article, apply a hashtag filter that excludes it → `[FILTER]` notice + working clear control; clear → article reappears in pane and list; no selection → `> select an article to read`.
</verification>

<success_criteria>
- READ-03: switching articles resets the reading-pane scroll to the top.
- READ-04: a filter that hides the selected article surfaces a notice with a restore/clear control (no silent mismatch, no auto-slam-shut).
- READ-01/READ-02 reconfirmed: sanitized Markdown body renders; placeholder shows when nothing is selected.
</success_criteria>

<output>
Create `.planning/phases/06-layout-scaffold-routing/06-05-SUMMARY.md` when done.
</output>
