---
phase: 06-layout-scaffold-routing
plan: 07
type: execute
wave: 7
depends_on: [06-04, 06-05, 06-06]
files_modified: []
autonomous: false
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, LINK-01, LINK-02, LINK-03, ENRICH-01, ROW-01, ROW-02, READ-01, READ-02, READ-03, READ-04, READ-05, MOBILE-01, MOBILE-02, MOBILE-03]
must_haves:
  truths:
    - "All 11 Phase 6 success criteria are confirmed TRUE in a real browser"
    - "rehype-sanitize is verified intact — no <script> survives in the rendered article DOM"
    - "Author images are confirmed https-only, no-referrer, lazy, and hidden on error in the live DOM/network tab"
    - "Mobile list scroll position is confirmed preserved across a back round-trip at 375px"
  artifacts: []
  key_links: []
---

<objective>
Final human verification of the complete v1.2 email-client layout. An automated pre-flight (build + tests + DOM/security greps) runs first; then a single blocking human-verify checkpoint walks all 11 Phase 6 success criteria in a browser across desktop and mobile viewports.

Purpose: Phases 06-01..06-06 deliver the full layout in slices; this plan proves the integrated whole satisfies every roadmap success criterion before the phase is marked complete. It supersedes the original 06-04 checkpoint (deleted when the phase was consolidated).
Output: A verification record (the SUMMARY) and a human GO/NO-GO on all 11 criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-layout-scaffold-routing/06-CONTEXT.md
@.planning/research/PITFALLS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight — build, tests, static security/structure assertions</name>
  <files></files>
  <read_first>
    - src/components/ArticleBody.tsx (confirm rehype-sanitize still in the rehypePlugins array; no rehype-raw)
    - src/components/SidebarRow.tsx (confirm image hardening attributes present)
    - src/App.tsx (confirm scroll-reset key, responsive classes, suppressed handle)
    - .planning/research/PITFALLS.md ("Looks Done But Isn't" checklist)
  </read_first>
  <action>
    Run the full automated gate and capture results for the SUMMARY. This task is autonomous; the human checkpoint (Task 2) runs only after this passes:
    1. `npx vitest run` — all suites green (data layer + image guard).
    2. `npx tsc -b --noEmit` — no type errors.
    3. `npx vite build` — production build succeeds and `build/404.html` is written by postbuild (hash-routing deep-link / no-404 prerequisite, Pitfall 1).
    4. Security/structure greps (static stand-ins for the runtime checks; record pass/fail):
       - `grep -c "rehypeSanitize" src/components/ArticleBody.tsx` ≥ 1 AND `grep -rc "rehype-raw" src/` is 0 (Pitfall 8 — sanitization boundary intact).
       - `grep -rc "dangerouslySetInnerHTML" src/` is 0.
       - In `src/components/SidebarRow.tsx`: `referrerPolicy="no-referrer"`, `loading="lazy"`, and `onError` each present; image src routed through `safeImageUrl` (Pitfall 6/7).
       - `test ! -f src/components/ArticleCard.tsx` and `grep -rn "ArticleCard\|Accordion" src/components/ArticleList.tsx` empty (READ-05).
       - `grep -c "key={selectedNaddr}" src/App.tsx` is 1 (READ-03).
       - ResizableHandle carries `hidden md:` (MOBILE-03) and a `hidden md:flex` sidebar toggle exists (MOBILE-01).
    Record each result as PASS/FAIL in the SUMMARY. If any FAIL, stop and report — do not proceed to the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && npx vitest run && npx tsc -b --noEmit && npx vite build && test -f build/404.html && grep -c "rehypeSanitize" src/components/ArticleBody.tsx && test "$(grep -rc 'rehype-raw' src/ | awk -F: '{s+=$2} END{print s+0}')" = "0" && test "$(grep -rc 'dangerouslySetInnerHTML' src/ | awk -F: '{s+=$2} END{print s+0}')" = "0" && test ! -f src/components/ArticleCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - vitest, tsc, and vite build all exit 0; `build/404.html` exists.
    - `rehype-raw` and `dangerouslySetInnerHTML` appear nowhere under `src/`.
    - `rehypeSanitize` present in ArticleBody; ArticleCard deleted; no Accordion/ArticleCard in ArticleList.
    - SidebarRow image hardening greps all pass.
    - `key={selectedNaddr}` present once in App.tsx; ResizableHandle is `hidden md:`.
  </acceptance_criteria>
  <done>The full automated gate passes and every static security/structure assertion is recorded PASS in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all 11 Phase 6 success criteria</name>
  <action>Perform the browser verification described in the checkpoint blocks below. This task is non-autonomous: pause and present the how-to-verify steps to the developer, who confirms each of the 11 ROADMAP Phase 6 success criteria on desktop and at 375px mobile. Do not auto-approve.</action>
  <verify><human-check>Developer confirms all 11 success criteria pass in a browser (see how-to-verify).</human-check></verify>
  <done>The developer has explicitly approved all 11 Phase 6 success criteria, or itemized the failing criteria for gap closure.</done>
  <what-built>
    The complete v1.2 email-client layout: a resizable 2-pane desktop split with deep-link routing, a full sanitized-Markdown reading pane with scroll-reset and hidden-by-filter handling, enriched inbox-style sidebar rows with hardened author images and a selected-row highlight, and a mobile single-pane list/reader swap with a back control and no drag handle.
  </what-built>
  <how-to-verify>
    Start the app: `npm run dev` and open the printed localhost URL. Wait for relays to stream articles in. Then verify each criterion (numbered to the ROADMAP Phase 6 success criteria):

    DESKTOP (wide window, ≥ md):
    1. (LAYOUT-01/02) You see a 2-pane horizontal split — sidebar left, reading pane right — filling the full viewport. With 20+ articles loaded, the sidebar list scrolls independently of the reading pane (scroll one; the other does not move). No page-level scrollbar.
    2. (LAYOUT-04/03) All filter controls are inside the sidebar: hashtag facets, the Match ALL/ANY (AND/OR) toggle, the ML on/off toggle, the confidence slider, download progress, and hidden count — all visible and functional. Terminal green-on-black/monospace aesthetic is unchanged across both panes.
    3. (LINK-01/03) Click an article; copy the URL from the address bar (it has a `#naddr1…` hash). Open it in a NEW TAB → the same article opens directly. Hard-reload (Cmd/Ctrl+Shift+R) that tab → it still opens (no 404).
    4. (LINK-02/LINK-03) Click several articles in turn, then use the browser Back and Forward buttons → the reading pane follows your selection history. Open a deep link in a fresh tab BEFORE articles finish streaming (reload and immediately observe) → you see `> resolving article from relays…`, then it resolves to the article automatically. (Optional: a hash for a non-existent article eventually shows `[404] article not found on connected relays`.)
    5. (READ-01/READ-05) Clicking a row shows that article's Markdown body in the right pane. Open DevTools console and run `document.querySelectorAll('script').length` after the body renders, or search the Elements panel within the article content for `<script>` → there must be NONE inside the rendered article. There is no inline-accordion expansion in the list (rows select only; no body opens inside the sidebar).
    6. (ENRICH-01/ROW-02) Each sidebar row shows: author avatar, author name, title, timestamp, a summary snippet, and a thumbnail when the article has an image. Find a row with no summary and/or no image → it renders cleanly with no broken/empty boxes and rows do not visibly jump as images load. The currently-open row is clearly highlighted.
    7. (ROW-01) Open DevTools → Network, filter to Img, reload. Confirm sidebar image requests are lazy (off-screen rows do not request until scrolled). Confirm no mixed-content warnings in the console. If any row has a dead/404 image, it is simply absent (no broken-image icon). (Author images are https-only, no-referrer, lazy.)
    8. (READ-02/03/04) With nothing selected the pane shows `> select an article to read`. Select article A, scroll its body halfway, then select article B → B shows from the TOP. Re-select A, then apply a hashtag filter that excludes A → the pane shows `[FILTER] this article is hidden by the current filter` with a `> clear filters` button; click it → A reappears in both list and pane.

    MOBILE (resize window to 375px wide, or DevTools device toolbar at 375px):
    9. (MOBILE-01) The article list fills the full screen; the reading pane is not shown. Tap a row → it transitions to a full-screen reading pane for that article.
    10. (MOBILE-02) The mobile reader shows a `‹ back` control. Before tapping a row, scroll the list down ~15 articles; tap article ~15; in the reader tap `‹ back` → you return to the list still scrolled to ~article 15 (scroll preserved).
    11. (MOBILE-03) At 375px there is NO resize drag handle between/around the panes and no resize affordance. Resize the window back to wide (≥ md) → the desktop 2-pane split WITH the drag handle reappears and resizing works (drag the handle; sidebar clamps between ~25% and ~50%).

    Report any criterion that fails with what you observed.
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 criteria pass, or describe which criteria failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nostr relay → app (article body + image URLs) | Verification step explicitly re-checks the two untrusted-input surfaces hardened in 06-04 (images) and preserved in 06-05 (Markdown body) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Elevation of Privilege | Rendered article Markdown (XSS) | mitigate | Task 1 asserts rehype-sanitize present and rehype-raw/dangerouslySetInnerHTML absent; Task 2 step 5 confirms no `<script>` in the live rendered DOM (Pitfall 8) |
| T-06-07-02 | Information Disclosure | Sidebar author images (tracking/mixed-content) | mitigate | Task 2 step 7 confirms lazy loading, no mixed-content warnings, and dead images hidden — the runtime confirmation of the 06-04 hardening (Pitfall 6/7) |
</threat_model>

<verification>
- Task 1 automated gate exits 0 with all security/structure greps PASS.
- Task 2 human checkpoint returns "approved" for all 11 criteria (or itemized failures routed back to the relevant plan's gap closure).
</verification>

<success_criteria>
- All 18 Phase 6 requirements (LAYOUT-01..04, LINK-01..03, ENRICH-01, ROW-01..02, READ-01..05, MOBILE-01..03) are confirmed satisfied by the integrated build.
- All 11 ROADMAP Phase 6 success criteria are confirmed TRUE by a human in a browser.
- The two untrusted-input surfaces (Markdown body, author images) are re-verified safe at runtime.
</success_criteria>

<output>
Create `.planning/phases/06-layout-scaffold-routing/06-07-SUMMARY.md` recording: the automated gate results (PASS/FAIL per check), the human verdict per criterion, and any follow-up gaps.
</output>
