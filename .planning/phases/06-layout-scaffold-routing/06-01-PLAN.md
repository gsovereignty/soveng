---
phase: 06-layout-scaffold-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nostr.ts
  - src/lib/nostr.test.ts
  - src/index.css
  - src/components/ui/resizable.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [LAYOUT-02, LINK-01]
must_haves:
  truths:
    - "articleNaddr(article) returns a NIP-19 naddr string for a kind:30023 article and never throws on malformed input"
    - "html, body, #root resolve to full viewport height so a child h-full/h-screen chain does not collapse to zero"
    - "ResizablePanelGroup/ResizablePanel/ResizableHandle are importable from @/components/ui/resizable"
  artifacts:
    - path: "src/lib/nostr.ts"
      provides: "articleNaddr helper wrapping naddrEncode with coordinate fallback"
      contains: "articleNaddr"
    - path: "src/lib/nostr.test.ts"
      provides: "Tests asserting naddr encode + malformed-input fallback"
      contains: "articleNaddr"
    - path: "src/index.css"
      provides: "height:100% chain on html/body/#root + overflow:hidden page guard"
      contains: "#root"
    - path: "src/components/ui/resizable.tsx"
      provides: "shadcn Resizable primitive (react-resizable-panels)"
      contains: "ResizablePanelGroup"
  key_links:
    - from: "src/lib/nostr.ts"
      to: "nostr-tools/nip19"
      via: "naddrEncode subpath import"
      pattern: "from [\"']nostr-tools/nip19[\"']"
---

<objective>
Lay the three non-App.tsx foundations every later plan in this phase depends on: the
`articleNaddr` deep-link helper, the `index.css` full-height chain (P9), and the shadcn
`resizable` primitive. None of these touch `src/App.tsx`, so this plan runs first and in
isolation; Plans 02 and 03 build on top of it.

Purpose: The height chain must exist before any scroll behavior is testable (P9 — `h-full`
collapses to zero without an explicit-height ancestor), the `resizable` primitive must be
installed before the panel layout can be written, and `articleNaddr` is the encode/decode
key both routing and the reading-pane stub key off (LINK-01).
Output: `articleNaddr` in `src/lib/nostr.ts` (+ tests), height rules in `src/index.css`,
`src/components/ui/resizable.tsx` plus the `react-resizable-panels` dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-scaffold-routing/06-CONTEXT.md
@.planning/phases/06-layout-scaffold-routing/06-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add articleNaddr helper to src/lib/nostr.ts (TDD)</name>
  <files>src/lib/nostr.ts, src/lib/nostr.test.ts</files>
  <read_first>
    - src/lib/nostr.ts (the file being modified — note existing articleCoordinate at lines 4–7 and the pure-helper style to mirror)
    - src/lib/nostr.test.ts (existing test conventions for this file — match the describe/it style and fixtures)
    - src/components/ArticleCard.tsx:7,20–24,46–51 (the `import { npubEncode } from "nostr-tools/nip19"` subpath pattern + the encode try/catch fallback discipline from decision 03-01)
    - src/types/nostr.ts:5–17 (Article type — confirm `pubkey`, `d`, and `coordinate` fields are present)
    - .planning/phases/06-layout-scaffold-routing/06-PATTERNS.md (section "src/lib/nostr.ts — articleNaddr helper", lines 110–148 — the exact signature and fallback to replicate)
  </read_first>
  <behavior>
    - Given an Article with a valid hex pubkey and a non-empty `d`, articleNaddr returns a string beginning with "naddr1".
    - Given an Article whose pubkey/d round-trips, decoding the result (naddrDecode) yields kind 30023, the same pubkey, and the same identifier — proving kind:30023 + pubkey + d-tag encoding (LINK-01).
    - Given an Article with a malformed pubkey that makes naddrEncode throw, articleNaddr does NOT throw and returns the article's `coordinate` string instead.
  </behavior>
  <action>
    Add `articleNaddr(article: Article): string` to `src/lib/nostr.ts`. Import `naddrEncode`
    from `nostr-tools/nip19` (subpath import only — CLAUDE.md forbids the root barrel; add it
    alongside the existing imports, do not import from `"nostr-tools"`). Body calls
    `naddrEncode({ kind: 30023, pubkey: article.pubkey, identifier: article.d })` inside a
    try/catch; on throw, return `article.coordinate` (the stable "30023:pubkey:d" key that
    never throws). Omit relay hints from the AddressPointer (privacy + shorter URLs per
    STACK/PITFALLS). Mirror the try/catch fallback discipline already used for `npubEncode` in
    ArticleCard. Write the tests in `src/lib/nostr.test.ts` first (RED), import `naddrDecode`
    from `nostr-tools/nip19` for the round-trip assertion, then implement (GREEN). Do NOT
    modify any existing export in nostr.ts.
  </action>
  <verify>
    <automated>npx vitest run src/lib/nostr.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/nostr.test.ts` passes including the three new behaviors above.
    - `grep -n "from \"nostr-tools/nip19\"" src/lib/nostr.ts` matches (subpath import present); `grep -c "from \"nostr-tools\"" src/lib/nostr.ts` returns 0 (no root barrel).
    - `grep -c "export function articleNaddr" src/lib/nostr.ts` returns 1.
    - The malformed-input test asserts articleNaddr returns the `coordinate` fallback rather than throwing.
  </acceptance_criteria>
  <done>articleNaddr is exported, encodes valid articles to naddr1… round-tripping to kind:30023+pubkey+identifier, and falls back to coordinate on encode failure; all nostr.test.ts cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the full-height chain in src/index.css (P9)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (the file being modified — the @layer base block at lines 45–58, specifically the `body { … min-height: 100vh }` rule at lines 49–57)
    - .planning/phases/06-layout-scaffold-routing/06-PATTERNS.md (section "src/index.css — height chain (P9)", lines 178–206 — the exact rules to add)
    - .planning/research/PITFALLS.md (P9 — why h-full collapses to zero without an explicit-height ancestor)
  </read_first>
  <action>
    Inside the existing `@layer base` block in `src/index.css`, add a rule setting
    `height: 100%` on `html, body, #root` so the descendant `h-screen`/`h-full` chain in
    AppShell resolves (P9). Add `overflow: hidden` on `html, body` so the page itself never
    scrolls — scrolling lives inside each ResizablePanel (prevents a double scrollbar). Leave
    the existing `body` declaration's other properties intact; the `min-height: 100vh` may
    stay (it coexists with `height: 100%`). Do not touch the `@theme inline` blocks, the CRT
    keyframes, or the accordion keyframes.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*/\*' src/index.css | grep -A2 'html, body, #root' | grep -c 'height: 100%'</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contains a rule `html, body, #root { height: 100%; }` inside `@layer base`.
    - `src/index.css` contains `overflow: hidden` applied to `html, body` (page-scroll guard).
    - `npm run build` still succeeds (the Tailwind v4 `@import "tailwindcss"` + `@theme` blocks remain valid).
  </acceptance_criteria>
  <done>html, body, and #root are height:100% and the page has overflow:hidden, so a child h-screen flex shell can establish independent inner scroll regions.</done>
</task>

<task type="auto">
  <name>Task 3: Install the shadcn resizable primitive</name>
  <files>src/components/ui/resizable.tsx, package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/06-layout-scaffold-routing/06-PATTERNS.md (section "src/components/ui/resizable.tsx — shadcn add", lines 279–286 — the exact CLI command and expected exports)
    - .planning/research/STACK.md (react-resizable-panels@4.11.2 confirmation and the `npx shadcn@latest add resizable` instruction)
    - CLAUDE.md ("use existing shadcn components, do not create new UI components" — resizable is the sanctioned CLI-generated primitive; do not hand-roll a flex/grid split)
    - package.json (confirm react-resizable-panels is NOT already present before installing)
  </read_first>
  <action>
    Run `npx shadcn@latest add resizable` to generate `src/components/ui/resizable.tsx` and
    install `react-resizable-panels` (expected ~4.11.2). This is the only new UI primitive
    this phase and the only sanctioned new component per CLAUDE.md — do NOT hand-write a
    resizable component or a flex/grid split. The generated file must export
    `ResizablePanelGroup`, `ResizablePanel`, and `ResizableHandle`. Do not edit the generated
    file beyond what the CLI produces.
  </action>
  <verify>
    <automated>grep -c "react-resizable-panels" package.json && grep -Ec "ResizablePanelGroup|ResizablePanel|ResizableHandle" src/components/ui/resizable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/resizable.tsx` exists and exports `ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle`.
    - `grep -c "react-resizable-panels" package.json` returns ≥1 (dependency recorded).
    - `npx tsc --noEmit` reports no errors introduced by the new file.
  </acceptance_criteria>
  <done>The shadcn resizable primitive is installed and importable from @/components/ui/resizable, with react-resizable-panels recorded in package.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Article data → articleNaddr | `article.pubkey` / `article.d` originate from untrusted relay events; passed to naddrEncode |
| npm install (shadcn add) | `react-resizable-panels` is fetched and added to the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | articleNaddr | mitigate | naddrEncode is wrapped in try/catch; malformed pubkey/d returns the stable `coordinate` string instead of throwing and crashing the render (tested) |
| T-06-SC | Tampering | npm install of react-resizable-panels | mitigate | react-resizable-panels is a research-verified [LEGIT] dependency (RESEARCH SUMMARY, package size + version confirmed, shadcn-sanctioned). No [ASSUMED]/[SUS] packages introduced; no blocking checkpoint required |

This is a client-only static frontend. No backend, no secrets, no new network surface in this plan. articleNaddr produces a URL fragment only — it neither parses untrusted input as code nor renders HTML.
</threat_model>

<verification>
- `npx vitest run src/lib/nostr.test.ts` passes (articleNaddr behaviors).
- `npx tsc --noEmit` clean.
- `npm run build` succeeds with the new CSS height rules and resizable dependency.
</verification>

<success_criteria>
- articleNaddr exported, tested, with coordinate fallback on malformed input (LINK-01 foundation).
- html/body/#root height chain + page overflow guard in place (P9 / LAYOUT-02 foundation).
- shadcn resizable primitive installed and importable (LAYOUT-02 foundation).
</success_criteria>

<output>
Create `.planning/phases/06-layout-scaffold-routing/06-01-SUMMARY.md` when done.
</output>
