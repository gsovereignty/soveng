---
phase: 06-layout-scaffold-routing
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/App.tsx
  - src/components/FilterBar.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04]
must_haves:
  truths:
    - "On desktop the user sees a 2-pane horizontal split — sidebar left, reading pane right — filling the full viewport"
    - "All existing filter controls (ContentFilterControls + FilterBar) live in the sidebar panel and remain functional"
    - "The sidebar list and the reading pane scroll independently; the page itself does not scroll"
    - "The terminal/CRT aesthetic (scanlines, flicker, phosphor) covers both panes"
  artifacts:
    - path: "src/App.tsx"
      provides: "h-screen flex shell hosting ResizablePanelGroup with sidebar (pinned header + scrolling list) and reading-pane placeholder"
      contains: "ResizablePanelGroup"
    - path: "src/components/FilterBar.tsx"
      provides: "FilterBar with sticky positioning removed (D-05)"
      contains: "border-b border-terminal-border"
  key_links:
    - from: "src/App.tsx"
      to: "@/components/ui/resizable"
      via: "import ResizablePanelGroup/ResizablePanel/ResizableHandle"
      pattern: "from [\"']@/components/ui/resizable[\"']"
    - from: "src/App.tsx sidebar panel"
      to: "scrollable article list region"
      via: "inner overflow-y-auto wrapper (P4 / #3548)"
      pattern: "overflow-y-auto"
---

<objective>
Replace AppShell's centered single-column layout with the desktop 2-pane master-detail
shell: a full-viewport `h-screen` flex container hosting a `ResizablePanelGroup`, sidebar
panel (35% default, clamped 25–50%) on the left with a pinned controls header + an
independently-scrolling article list below it, and a reading-pane panel (65%) on the right
holding a placeholder for now. Relocate `ContentFilterControls` + `FilterBar` into the
sidebar pinned header and drop FilterBar's now-unnecessary sticky wrapper.

Purpose: This is the LAYOUT-01..04 visual shell — the structural skeleton routing (Plan 03)
will populate. It deliberately stops short of routing/selection so the height chain (P9) and
the ResizablePanel inner-scroll fix (P4/#3548) can be verified in isolation before selection
logic is layered on. The frozen memo chain (sortedArticles → … → filteredArticles) is NOT
touched.
Output: Reworked `src/App.tsx` shell + repositioned controls; de-stickied `FilterBar`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-scaffold-routing/06-CONTEXT.md
@.planning/phases/06-layout-scaffold-routing/06-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/06-layout-scaffold-routing/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop sticky positioning from FilterBar (D-05)</name>
  <files>src/components/FilterBar.tsx</files>
  <read_first>
    - src/components/FilterBar.tsx (the file being modified — root wrapper div at line 30 carrying `sticky top-0 z-20`)
    - .planning/phases/06-layout-scaffold-routing/06-PATTERNS.md (section "src/components/FilterBar.tsx — reposition only", lines 152–164 — drop sticky, keep everything else verbatim)
    - .planning/phases/06-layout-scaffold-routing/06-CONTEXT.md (D-05 — sticky-in-a-scroll-container was the research-flagged fragility; removing it is the fix, not preserving it)
  </read_first>
  <action>
    In `src/components/FilterBar.tsx` remove the classes `sticky top-0 z-20` from the root
    `<div>` (currently line 30). Keep the remaining classes
    `bg-terminal-bg border-b border-terminal-border py-2 px-0 mb-4` exactly. Make NO other
    change — props, the ToggleGroup match-mode control, the Checkbox facet list, and the
    `> more (n)` expander stay verbatim. This is a reposition-only edit per D-05; the controls
    now live in a pinned `shrink-0` header (added in Task 2) so sticky is unnecessary.
  </action>
  <verify>
    <automated>grep -c "sticky" src/components/FilterBar.tsx; grep -c "border-b border-terminal-border" src/components/FilterBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sticky" src/components/FilterBar.tsx` returns 0 (sticky fully removed).
    - `grep -c "border-b border-terminal-border" src/components/FilterBar.tsx` returns ≥1 (border kept).
    - No change to the FilterBar props interface (diff touches only the root className string).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>FilterBar no longer carries `sticky top-0 z-20`; all other markup and props are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AppShell as the 2-pane Resizable shell with sidebar controls + placeholder reading pane</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (the file being modified — the full memo chain lines 37–83 which MUST NOT change, the existing status branch ladder lines 103–165, and the outer wrapper being replaced at lines 95–101)
    - .planning/phases/06-layout-scaffold-routing/06-PATTERNS.md (section "src/App.tsx — AppShell", patterns 4 "Outer-wrapper + layout swap" lines 77–91 and 5 "Terminal status-conditional render (preserve)" lines 93–97)
    - .planning/phases/06-layout-scaffold-routing/06-CONTEXT.md (D-01 35/65 split, D-02 minSize 25/maxSize 50, D-04 pinned-header + scrolling-list sidebar layout, code_context "Integration Points")
    - .planning/research/ARCHITECTURE.md ("Layout Structure" — the h-screen flex chain and panel sizing)
    - .planning/research/PITFALLS.md (P4 ResizablePanel overflow:hidden #3548 — inner overflow-y-auto wrapper; P9 h-full height chain)
    - src/components/ui/resizable.tsx (the generated primitive — confirm ResizablePanelGroup/ResizablePanel/ResizableHandle prop shapes: defaultSize, minSize, maxSize, withHandle)
  </read_first>
  <action>
    Replace AppShell's outer wrapper. Change the current
    `className="crt-scanlines crt-flicker min-h-screen bg-terminal-bg flex flex-col items-center justify-center p-8"`
    centered column (lines 95–101) to a full-viewport shell
    `className="crt-scanlines crt-flicker h-screen bg-terminal-bg flex flex-col overflow-hidden"`
    so the CRT chrome (LAYOUT-03) covers both panes and the height chain (P9) resolves. Keep
    the existing top-level status branches (`status === "streaming" && articles.length === 0`
    → BootSequence; `status === "error"` → `[ERR]`; `status === "empty"` → `[EMPTY]`) — those
    render full-shell as today. In the articles-present branch, render
    `ResizablePanelGroup` (import from `@/components/ui/resizable`) with
    `direction="horizontal"` filling `flex-1`. LEFT `ResizablePanel`
    `defaultSize={35} minSize={25} maxSize={50}` (D-01/D-02): inside it a flex-col column with
    a `shrink-0` pinned header containing `ContentFilterControls` then `FilterBar` (moved here
    verbatim with the same props they receive today), and below the header a
    `<div className="flex-1 overflow-y-auto">` region (P4 inner-scroll wrapper — never put
    overflow-y-auto directly on the panel) containing the stub article list. For the stub list
    reuse the existing `<ArticleList articles={filteredArticles} profiles={profiles}
    status={status} />` (per D-discretion: ArticleList is acceptable as the stub this phase and
    is deleted in Phase 7) — keep the existing `isFilterEmpty` `[FILTER]` branch inside this
    scroll region. Add `ResizableHandle withHandle` between the panels. RIGHT `ResizablePanel`
    (reading pane, takes remainder ≈65%): inside it a `<div className="flex-1 overflow-y-auto">`
    wrapper (P4) containing a temporary centered placeholder line styled
    `font-mono text-sm text-terminal-muted` reading `> select an article to read` (the real
    reading-pane stub + its states arrive in Plan 03 — leave the placeholder static here, NO
    selection logic). Do NOT add any selectedNaddr/hash/selectedArticle code in this plan. Do
    NOT modify the memo chain (lines 37–83), `onTagToggle`, or the existing handlers. Keep the
    footer line ("powered by nostr…") if it still fits the shell, or drop it into the sidebar
    header — your discretion within the terminal aesthetic.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "ResizablePanelGroup" src/App.tsx && grep -c "min-h-screen" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` imports `ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle` from `@/components/ui/resizable`.
    - The outer wrapper className contains `h-screen` and `overflow-hidden`; `grep -c "min-h-screen" src/App.tsx` returns 0 (old centered layout gone).
    - The left ResizablePanel has `defaultSize={35}`, `minSize={25}`, `maxSize={50}` (D-01/D-02 exact values).
    - `ContentFilterControls` and `FilterBar` render inside a `shrink-0` sidebar header; the article list sits in a sibling `flex-1 overflow-y-auto` region (P4 — overflow-y-auto is on an inner div, never on the ResizablePanel itself).
    - The reading-pane panel wraps its content in an inner `overflow-y-auto` div and shows the static `> select an article to read` placeholder.
    - The memo chain (sortedArticles, visibleArticles, facets, dynamicCounts, filteredArticles) is byte-for-byte unchanged (lines 37–83).
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
    - No `selectedNaddr`, `window.location.hash`, `hashchange`, or `selectedArticle` references exist in App.tsx yet (those are Plan 03).
  </acceptance_criteria>
  <done>AppShell renders a full-viewport 2-pane Resizable split: sidebar (pinned ContentFilterControls + FilterBar header over an independently-scrolling article list, 35% default, 25–50% clamp) and a reading-pane panel with an inner-scroll placeholder, CRT aesthetic across both, page itself not scrolling, memo chain untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay article data → sidebar list render | Untrusted article fields flow into the (reused) ArticleList; no NEW rendering surface added here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | reading-pane placeholder | accept | The reading pane in this plan renders a static terminal string only — no article content, no Markdown, no raw HTML. No new untrusted-data render path is introduced (Markdown body lands in Phase 7) |
| T-06-03 | Tampering | layout refactor | accept | Presentation-only rearrangement; ArticleList/ArticleBody sanitization behavior is unchanged (components reused verbatim, not rewritten) |

This is a client-only static frontend presentation refactor. No backend, no secrets, no new network or input-parsing surface. The reading pane is a placeholder string in this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` succeeds.
- `npm run dev` then load with 20+ articles: a 2-pane split renders; dragging the handle resizes within the 25–50% sidebar clamp; the sidebar list and reading pane each scroll independently while the page does not.
- Filter controls in the sidebar still filter the list (facet checkboxes, AND/OR toggle, ML toggle, slider all functional).
</verification>

<success_criteria>
- Desktop 2-pane horizontal split fills the viewport, both regions scrolling independently (LAYOUT-01, LAYOUT-02).
- All filter controls live and functional in the sidebar (LAYOUT-04).
- CRT/terminal aesthetic preserved across both panes (LAYOUT-03).
- Memo chain and data layer untouched.
</success_criteria>

<output>
Create `.planning/phases/06-layout-scaffold-routing/06-02-SUMMARY.md` when done.
</output>
