---
phase: 05-ml-pipeline-infrastructure
plan: 05
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - src/App.tsx
autonomous: true
requirements: [CTRL-01, CTRL-03, CTRL-04, CTRL-05, LEN-01, MLINF-03, SPAM-04]
must_haves:
  truths:
    - "Articles render immediately and spam/non-English ones disappear progressively as results arrive (MLINF-03, no blocking)"
    - "The visibleArticles memo is the single ML-filter insertion point between sortedArticles and the three downstream memos"
    - "Facets, dynamic counts, and filtered list all derive from visibleArticles (never diverge from what is shown)"
    - "Content filtering is ON by default and the toggle state persists across reloads via localStorage (CTRL-01, CTRL-04)"
    - "The toggle governs spam + language only; the 500-word length gate stays hidden regardless of the toggle (D-06, LEN-01)"
    - "The slider re-thresholds the visible list instantly with no re-inference (CTRL-05, D-02)"
    - "The hidden-articles count and model-download progress and failure notice are surfaced to the controls (CTRL-02/03)"
  artifacts:
    - path: "src/App.tsx"
      provides: "useClassification wiring + visibleArticles memo + ML UI state + ContentFilterControls render"
      contains: "visibleArticles"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useClassification.ts"
      via: "useClassification(sortedArticles)"
      pattern: "useClassification\\("
    - from: "src/App.tsx"
      to: "src/components/ContentFilterControls.tsx"
      via: "rendered alongside FilterBar"
      pattern: "ContentFilterControls"
---

<objective>
Wire the classification pipeline into the live app: call `useClassification(sortedArticles)`, insert the
`visibleArticles` memo as the single ML-filter insertion point between `sortedArticles` and the three existing
downstream memos (facets / dynamicCounts / filteredArticles), switch those three to consume `visibleArticles`,
add the ML UI state (filter toggle persisted to localStorage default-on, live spam threshold), and render
`ContentFilterControls` alongside `FilterBar`. This is the final integration step that makes filtering visible
and controllable end-to-end.

Implements CTRL-01 (on by default), CTRL-04 (toggle persisted to localStorage), CTRL-05 (live threshold slider),
CTRL-02/CTRL-03 (progress + count surfaced), LEN-01/D-06 (length gate hides regardless of toggle), MLINF-03
(progressive, cached), SPAM-04 (fail-open preserved by isHidden allowlist).

Purpose: Single-file, surgical integration — App.tsx is the only structural change, per the research seam.
Output: `src/App.tsx` (modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ml-pipeline-infrastructure/05-CONTEXT.md
@.planning/phases/05-ml-pipeline-infrastructure/05-PATTERNS.md
@.planning/phases/05-ml-pipeline-infrastructure/05-03-SUMMARY.md
@.planning/phases/05-ml-pipeline-infrastructure/05-04-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert visibleArticles memo, ML UI state, and switch downstream memos</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (the file being modified — the exact memo pipeline lines 20-44, local UI state lines 14-16, the FilterBar render block)
    - .planning/phases/05-ml-pipeline-infrastructure/05-PATTERNS.md (the "src/App.tsx (modify — additive)" 5-step insertion: imports, ML state, visibleArticles memo, downstream switch, props threading)
    - .planning/phases/05-ml-pipeline-infrastructure/05-03-SUMMARY.md (the useClassification return contract: map/version/scores/downloadProgress/modelFailed)
    - .planning/research/ARCHITECTURE.md (Pattern 5: visibleArticles as the single insertion point; facets-from-visibleArticles decision; anti-pattern 5: ML filter before hashtag filter)
    - src/types/nostr.ts (isHidden helper from Plan 01)
  </read_first>
  <action>
    Modify `src/App.tsx` additively, preserving all existing structure (the status branches, BootSequence, error/empty/empty-filter arms, FilterBar). (1) Add imports: `useEffect` (for localStorage persistence), `useClassification` from `@/hooks/useClassification`, `isHidden` from `@/types/nostr`, `ContentFilterControls` from `@/components/ContentFilterControls`. (2) Add ML UI state alongside selectedTags/matchMode: `filterEnabled` via lazy initializer reading `localStorage.getItem('soveng:ml-filter-enabled') !== 'false'` (CTRL-01 default ON), and `spamThreshold` useState default `0.90` (D-02); add a `useEffect` that persists `filterEnabled` to localStorage key `soveng:ml-filter-enabled` on change (CTRL-04). (3) Call `const { map: classificationMap, version: classificationVersion, downloadProgress, modelFailed } = useClassification(sortedArticles)` passing the active `spamThreshold` per the Plan 03 contract so the slider re-thresholds. (4) Insert the `visibleArticles` useMemo BETWEEN `sortedArticles` and `facets`: when `filterEnabled` is false, return `sortedArticles.filter(a => classificationMap.get(a.id) !== 'short')` (toggle OFF still hides sub-500-word via the always-on length gate, D-06); when true, return `sortedArticles.filter(a => !isHidden(classificationMap.get(a.id)))` (fail-open allowlist). Dependency list MUST include `classificationVersion`, `filterEnabled`, AND `spamThreshold` so re-thresholding re-evaluates (D-02). (5) Switch the three downstream memos (`facets`, `dynamicCounts`, `filteredArticles`) to consume `visibleArticles` instead of `sortedArticles` — one input change each, preserving structure and dep lists (swap sortedArticles->visibleArticles in each dep array). (6) Compute `filteredCount = sortedArticles.length - visibleArticles.length` (hidden count, CTRL-03). (7) Render `<ContentFilterControls>` adjacent to `<FilterBar>` inside the articles branch, passing filterEnabled/onFilterEnabledChange (setFilterEnabled), spamThreshold/onSpamThresholdChange (setSpamThreshold), filteredCount, downloadProgress, modelFailed per the Plan 04 props contract. Do NOT alter the data-layer NostrContext, reducer, or fetch hooks (anti-pattern 1). Do NOT apply the ML filter after the hashtag filter (anti-pattern 5 — visibleArticles is upstream of facets/filter).
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && grep -q "useClassification(sortedArticles" src/App.tsx && grep -q "visibleArticles" src/App.tsx && grep -q "buildFacets(visibleArticles)" src/App.tsx && grep -q "soveng:ml-filter-enabled" src/App.tsx && grep -q "ContentFilterControls" src/App.tsx && npm run build 2>&1 | tail -2 && npm test 2>&1 | grep -qiE "(pass|✓)" && echo APP_INTEGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx calls `useClassification(sortedArticles, ...)` and passes the active spamThreshold
    - `visibleArticles` memo exists between sortedArticles and facets; its dep array includes classificationVersion, filterEnabled, and spamThreshold
    - All three downstream memos now read `visibleArticles` (grep: buildFacets(visibleArticles), computeDynamicCounts(visibleArticles, ...), filterArticles(visibleArticles, ...))
    - filterEnabled defaults to true when localStorage key absent; persists via useEffect to `soveng:ml-filter-enabled`
    - When filterEnabled is false, only `'short'` (length-gate) articles remain hidden (D-06); spam/non-english are shown
    - filteredCount = sortedArticles.length - visibleArticles.length is passed to the controls
    - ContentFilterControls is rendered in the articles branch with all seven props wired
    - The NostrContext/reducer/fetch hooks are unchanged (no edits outside App.tsx)
    - `npm run build` exits 0 and `npm test` passes (existing suites stay green)
  </acceptance_criteria>
  <done>useClassification is wired, visibleArticles is the single ML insertion point feeding the three downstream memos, the toggle (default-on, persisted) and slider control spam+language while the length gate stays always-on, and the controls show count/progress/failure — build and tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classification results → render decision | App decides what to hide based on the classification map; must remain fail-open |
| localStorage → filterEnabled init | persisted toggle string is read at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-VISFAIL | Tampering | visibleArticles filter | mitigate | Use isHidden allowlist (only spam/non-english/short hide); undefined/pending/error/ham show — a classification fault never empties the list (SPAM-04 fail-open preserved at the render seam) |
| T-05-LSPARSE | Tampering | localStorage filterEnabled read | mitigate | Treat any value other than the literal `'false'` as enabled (default-on, CTRL-01); a corrupted/absent key yields the safe default rather than throwing |
| T-05-LENBYPASS | Tampering | length gate vs toggle | mitigate | When the toggle is OFF the memo still hides `'short'` articles (D-06/LEN-01) — the editorial length rule cannot be disabled via the toggle |
</threat_model>

<verification>
- `npm run build` exits 0; `npm test` green (no regressions in existing reducer/facets/nostr/timestamp suites)
- grep confirms all three downstream memos consume visibleArticles
- grep confirms localStorage key and ContentFilterControls render
</verification>

<success_criteria>
The app renders all articles immediately, progressively hides spam/non-English as worker results stream in, always hides sub-500-word notes, persists an on-by-default toggle, re-thresholds instantly via the slider, and surfaces hidden count + download progress + failure notice — with App.tsx as the only file changed and no data-layer edits.
</success_criteria>

<output>
Create `.planning/phases/05-ml-pipeline-infrastructure/05-05-SUMMARY.md` when done.
</output>
