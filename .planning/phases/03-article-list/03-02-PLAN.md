---
phase: 03-article-list
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/ArticleList.tsx
  - src/App.tsx
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, DISP-05]
must_haves:
  truths:
    - "Real Nostr articles render end-to-end as terminal-styled cards on the page (and on the deployed Pages URL)"
    - "The boot screen plays only while zero articles have arrived; on the first article the view swaps to the live list and appends cards as events stream in (D-01)"
    - "A slim streaming status line above the list updates live (`> streaming… N/21 received`) and resolves to a done line when the stream stops (D-02)"
    - "Cards render in arrival order and are never re-sorted; no visible reorder/jump on done (D-03)"
    - "A distinct loading state (boot), empty state, and relay-error state are shown — error ≠ no-matches — with the existing retry control preserved (DISP-05)"
  artifacts:
    - path: "src/components/ArticleList.tsx"
      provides: "Streaming status line + maps Article[] → ArticleCard, looking up each author profile"
      exports: ["ArticleList"]
      min_lines: 25
    - path: "src/App.tsx"
      provides: "Progressive boot-then-stream branch logic wiring ArticleList into the has-articles path; preserves error/empty/retry branches"
      contains: "ArticleList"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/ArticleList.tsx"
      via: "renders <ArticleList articles profiles status /> in the has-articles branch"
      pattern: "<ArticleList"
    - from: "src/components/ArticleList.tsx"
      to: "src/components/ArticleCard.tsx"
      via: "maps articles to <ArticleCard article profile />"
      pattern: "<ArticleCard"
    - from: "src/components/ArticleList.tsx"
      to: "profiles Map"
      via: "profiles.get(article.pubkey) per card"
      pattern: "profiles\\.get\\("
    - from: "src/App.tsx"
      to: "useNostr profiles"
      via: "destructure profiles from useNostr()"
      pattern: "profiles"
---

<objective>
Compose `ArticleCard`s into a live streaming `ArticleList` and wire it into `App.tsx`,
completing the Phase 3 vertical slice: real Nostr articles render end-to-end on the page.
The boot screen plays only at zero articles, then the view swaps to the list and appends
cards as relay events stream in (D-01), with a slim live status line (D-02) and arrival-order
rendering with no re-sort (D-03). The existing empty and relay-error states with their retry
controls are preserved exactly (DISP-05) — Phase 3 must not regress Phase 2 work.

Purpose: Realize the phase goal — real articles rendered with title/author/timestamp and
correct loading/empty/error states — and complete DISP-01/02/03/05 in the integrated app.
Output: `src/components/ArticleList.tsx`; modified `src/App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-list/03-CONTEXT.md
@.planning/phases/03-article-list/03-PATTERNS.md
@.planning/phases/03-article-list/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ArticleList (streaming status line + card mapping)</name>
  <files>src/components/ArticleList.tsx</files>
  <read_first>
    - src/components/ArticleCard.tsx (the card component + its prop signature { article, profile } — built in Plan 01)
    - src/components/BootSequence.tsx (terminal styling analog; max-w-2xl column, font-mono, crt-glow text)
    - src/context/NostrContext.tsx (useNostr() shape: { status, articles, profiles, refetch }; profiles is Map<string, Profile>)
    - src/types/nostr.ts (Article, Profile, NostrStatus)
    - src/index.css (terminal tokens: terminal-green-dim, terminal-muted; crt-glow helper)
    - .planning/phases/03-article-list/03-PATTERNS.md §"src/components/ArticleList.tsx" (prop signature, streaming/done status-line markup, map-over-articles pattern, plain div container not a Card)
  </read_first>
  <action>
    Create `src/components/ArticleList.tsx` exporting `ArticleList` with prop signature
    `{ articles: Article[]; profiles: Map<string, Profile>; status: NostrStatus }`.

    Imports (exact paths): `type { Article, Profile, NostrStatus }` from `@/types/nostr`;
    `{ ArticleCard }` from `@/components/ArticleCard`; `{ cn }` from `@/lib/utils`.

    Slim streaming status line (D-02, above the list): while `status === "streaming"`, render a
    one-line terminal header `> streaming… {articles.length}/21 received` using
    `crt-glow text-terminal-green-dim text-xs tracking-widest uppercase`. When
    `status !== "streaming"`, render the resolved done line
    `> ready — {articles.length} articles loaded` with the same classes. Wrap in a
    `max-w-2xl` header block (exact wording/format is Claude's discretion within the terminal aesthetic).

    List body (D-03 arrival order, no re-sort): render a plain `<div>` (NOT a Card — each
    ArticleCard is its own Card) in a `max-w-2xl` column with vertical spacing. Map over
    `articles` IN THE GIVEN ORDER — do not sort, reverse, or reorder; the array is already
    frozen in arrival order by Phase 2 (D-02). For each article render
    `<ArticleCard key={article.id} article={article} profile={profiles.get(article.pubkey)} />`.
    Passing `profiles.get(...)` (possibly `undefined`) realizes upgrade-in-place: cards render
    immediately and re-render with the avatar/name when the kind:0 profile arrives, with no
    loading gate.

    No virtualization (fixed ≤21 count). Do NOT implement filtering, hashtag sidebar, or
    click-to-expand — all Phase 4.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "<ArticleCard" src/components/ArticleList.tsx && grep -q "profiles.get(" src/components/ArticleList.tsx && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ArticleList.tsx` exports `ArticleList` and `npm run build` exits 0.
    - Card mapping assertion: file renders `<ArticleCard` and passes `profile={profiles.get(article.pubkey)}` (grep both present).
    - Streaming status assertion: file contains the streaming string `streaming…` / `received` and a done string `ready` (`grep -c "received" src/components/ArticleList.tsx` ≥ 1).
    - No re-sort assertion: file does NOT call `.sort(` on the articles array (`grep -v '^#' src/components/ArticleList.tsx | grep -c '\.sort('` returns 0).
    - Not a Card container: the list body is a plain element, not a wrapping `<Card>` around all items (the only Card-level rendering is inside ArticleCard).
  </acceptance_criteria>
  <done>ArticleList shows a live status line, maps articles in arrival order to ArticleCards, and resolves each author profile via profiles.get(pubkey) with no gating.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ArticleList into App.tsx with boot-then-stream branch logic</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (the file being modified — current status switch on lines 17-47; outer crt-scanlines wrapper, header, footer must be preserved; current done branch is a debug <pre>)
    - src/components/ArticleList.tsx (the component being wired in — built in Task 1; its prop signature)
    - src/context/NostrContext.tsx (useNostr() returns { status, articles, profiles, refetch }; profiles must be destructured)
    - .planning/phases/03-article-list/03-PATTERNS.md §"src/App.tsx" (exact current vs new branch logic; which branches are preserved verbatim)
    - .planning/phases/03-article-list/03-CONTEXT.md §decisions (D-01 boot-then-stream; "carried forward" lock: keep error/empty/retry exactly)
  </read_first>
  <action>
    Surgically modify `src/App.tsx`. Add import `{ ArticleList }` from `@/components/ArticleList`.
    Change the `useNostr()` destructure in `AppShell` to also pull `profiles`:
    `const { status, articles, profiles, refetch } = useNostr()`.

    Replace ONLY the `<main>` status-branch expression. New branch order (D-01, D-02 progressive
    boot-then-stream):
    1. `status === "streaming" && articles.length === 0` → `<BootSequence />` (boot plays only
       while zero articles collected).
    2. `status === "error"` → KEEP the existing error block EXACTLY (the `[ERR] relay connection
       failed …` paragraph + `> retry connection` button calling `refetch`). Do not edit copy or classes.
    3. `status === "empty"` → KEEP the existing empty block EXACTLY (the `[EMPTY] relays
       responded but no articles found` paragraph + `> retry fetch` button). Do not edit.
    4. else (articles.length > 0 — streaming-with-articles OR done) → render
       `<ArticleList articles={articles} profiles={profiles} status={status} />`. This replaces
       the debug `<pre>{status: …}</pre>` block entirely.

    Preserve UNCHANGED: the outer `<div className="crt-scanlines crt-flicker …">`, the `<header>`
    block (soveng // nostr long-form reader), and the `<footer>` block. Only the `<main>` content
    and the destructure/import change. Do NOT regress the error/empty/retry behavior (Phase 2
    D-06/D-12 lock) and do NOT introduce a loadingProfiles gate.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "<ArticleList" src/App.tsx && grep -q "articles.length === 0" src/App.tsx && grep -q "profiles" src/App.tsx && ! grep -q "status: \${status}" src/App.tsx && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - App renders ArticleList: `grep -c "<ArticleList" src/App.tsx` ≥ 1, passing `articles`, `profiles`, and `status` props.
    - Boot-then-stream assertion: branch guards on `status === "streaming" && articles.length === 0` (`grep -c "articles.length === 0" src/App.tsx` ≥ 1).
    - Debug `<pre>` removed: `grep -c "status: \${status}" src/App.tsx` returns 0.
    - Error/empty preserved: `grep -c "retry connection" src/App.tsx` ≥ 1 AND `grep -c "retry fetch" src/App.tsx` ≥ 1 (DISP-05 distinct states kept).
    - `profiles` destructured from useNostr: `grep -c "profiles" src/App.tsx` ≥ 1.
    - CRT wrapper preserved: `grep -c "crt-scanlines" src/App.tsx` ≥ 1.
  </acceptance_criteria>
  <done>App swaps from boot to live list on first article, renders ArticleList for the has-articles/done path, and keeps the distinct empty and error states with their retry controls intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nostr relay → app | Untrusted article/profile data already normalized by Phase 2; flows into ArticleList → ArticleCard for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering/Elevation (XSS) | ArticleList/App render of untrusted article fields | mitigate | ArticleList passes data as typed props; all untrusted-string rendering is delegated to ArticleCard (Plan 01) which uses React text children only — no `dangerouslySetInnerHTML` anywhere. |
| T-03-06 | Tampering | Untrusted article count / malformed array drives status line | accept | `articles.length` is a number from Phase 2's frozen-at-21 array; rendered as a numeric React child, not interpolated into markup. Low risk. |
| T-03-07 | Repudiation/Availability (regression) | App.tsx branch edit drops the relay-error or empty state | mitigate | Acceptance criteria assert both `retry connection` and `retry fetch` strings remain present (DISP-05 distinct states preserved); CRT wrapper presence asserted. |

Note: No package installs in this plan (no T-03-SC). DISP-04 Markdown body rendering and its
rehype-sanitize XSS surface are explicitly OUT of scope (Phase 4) — Phase 3 renders only short
metadata, so no Markdown/raw-HTML threat applies here.
</threat_model>

<verification>
- `npm run build` exits 0 (full app type-checks).
- App renders `<ArticleList>` in the has-articles branch with articles/profiles/status props.
- Boot plays only at zero articles; debug `<pre>` removed.
- Error and empty states with retry controls preserved (grep both retry strings).
- ArticleList maps cards in arrival order (no `.sort`) and resolves profiles via `profiles.get`.
</verification>

<success_criteria>
- Phase goal: real Nostr articles render end-to-end with title, author identity, and timestamp.
- DISP-05: distinct loading (boot), empty, and relay-error states; error ≠ no-matches; retry preserved.
- D-01: boot-then-stream swap on first article; cards append as events arrive.
- D-02: live streaming status line resolving to a done line.
- D-03: arrival-order rendering, no re-sort/jump on done.
</success_criteria>

<output>
Create `.planning/phases/03-article-list/03-02-SUMMARY.md` when done.
</output>
