---
phase: 05-ml-pipeline-infrastructure
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - src/hooks/useClassification.ts
autonomous: false
requirements: [SPAM-02, SPAM-04, MLINF-02]
must_haves:
  truths:
    - "Real spam scores for 20+ live Nostr articles are logged to the dev console (dev-only, not in production bundle) — D-04"
    - "On the deployed /soveng/ URL there are no .wasm 404s in the network tab (Pitfall 2, criterion 3)"
    - "On the deployed URL there is no SharedArrayBuffer warning in the console (Pitfall 5, numThreads=1, criterion 3)"
    - "The wasmPaths CDN version pin matches node_modules/onnxruntime-web/package.json (Pitfall 3, criterion 3)"
    - "A model-load failure leaves all articles visible with the non-blocking 'filter unavailable' notice (SPAM-04, criterion 4)"
    - "The go/no-go is made: trust the spam filter at the pinned threshold, OR fall back to language-only + length (D-01)"
  artifacts:
    - path: "src/hooks/useClassification.ts"
      provides: "dev-only score logging + final pinned SPAM_THRESHOLD (or language-only fallback per go/no-go)"
      contains: "SPAM_THRESHOLD"
  key_links:
    - from: "src/hooks/useClassification.ts"
      to: "console (dev-only)"
      via: "import.meta.env.DEV-guarded score log"
      pattern: "import\\.meta\\.env\\.DEV"
---

<objective>
Close the phase with the validation-gated go/no-go (D-01) and the live-URL deployment smoke test (success
criterion 3). Add dev-only spam-score logging (D-04), deploy, then verify on the real
https://gsovereignty.github.io/soveng/ URL that the four deployment constraints hold and that real
crypto/Nostr articles are not over-filtered at the default threshold. Based on the observed score
distribution, PIN the final `SPAM_THRESHOLD` — or, if legitimate articles cluster as spam even near 0.90,
fall back to language-only + length filtering for this milestone (defer spam ML to SPAM-05). This is the
explicit in-phase validation the user mandated: do not trust the spam filter until real scores are eyeballed.

Purpose: Prove the deployment constraints on the actual host (localhost cannot detect the /soveng/ sub-path
wasmPaths break) and make the evidence-based spam go/no-go decision.
Output: `src/hooks/useClassification.ts` (dev-logging + final threshold or fallback), and a recorded
validation verdict in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ml-pipeline-infrastructure/05-CONTEXT.md
@.planning/phases/05-ml-pipeline-infrastructure/05-PATTERNS.md
@.planning/phases/05-ml-pipeline-infrastructure/05-03-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@src/hooks/useClassification.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-only spam-score logging and deploy</name>
  <files>src/hooks/useClassification.ts</files>
  <read_first>
    - src/hooks/useClassification.ts (the file being modified — the worker-result handler where scores arrive)
    - .planning/phases/05-ml-pipeline-infrastructure/05-CONTEXT.md (D-04 — dev-console logging, dev-only not in production bundle)
    - .planning/research/PITFALLS.md (Pitfall 1 step 4 "log scores to console in dev"; Security row "log scores + ids only, never full text")
    - .planning/research/STACK.md (GitHub Actions deploy workflow — push to main triggers build+deploy)
  </read_first>
  <action>
    In `src/hooks/useClassification.ts`, in the worker-result handler, add a score log guarded by `import.meta.env.DEV` so it is tree-shaken out of the production bundle (D-04 — dev-only). Log ONLY event id (or short title), the raw spam score, and the resolved label — NEVER the article body text (Security: bodies may embed keys). Format it so 20+ articles' scores are easy to scan (e.g. console.table or one line per article with id, score, label, and whether it would hide at the current threshold). Then commit and push to main to trigger the GitHub Actions deploy (per STACK.md deploy workflow). Confirm the Actions run succeeds and the deployment publishes.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && grep -q "import.meta.env.DEV" src/hooks/useClassification.ts && npm run build 2>&1 | tail -2 && echo DEVLOG_OK</automated>
  </verify>
  <acceptance_criteria>
    - The score log is guarded by `import.meta.env.DEV` (absent from the production build)
    - The log includes id + score + label but NOT the article body/content text
    - `npm run build` exits 0
    - The GitHub Actions deploy run for the pushed commit completes successfully (Actions tab shows green)
  </acceptance_criteria>
  <done>Dev-only score logging is in place, the production bundle excludes it, and the latest build is deployed to the /soveng/ Pages URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live-URL deployment smoke test (success criterion 3)</name>
  <what-built>
    The full content-filtering feature is deployed to https://gsovereignty.github.io/soveng/. The worker
    loads the q8 SMS-spam ONNX model single-threaded (numThreads=1) with a version-pinned wasmPaths CDN URL,
    and the dev build logs real spam scores to the console for the loaded articles.
  </what-built>
  <how-to-verify>
    1. Open https://gsovereignty.github.io/soveng/ in a browser with DevTools open (Network + Console tabs).
    2. NETWORK: filter by "wasm" — confirm the ONNX `.wasm` file(s) load with HTTP 200 from the jsDelivr CDN; confirm there are NO 404s for any `.wasm` at the origin root (e.g. https://gsovereignty.github.io/ort-wasm*.wasm). (Pitfall 2)
    3. CONSOLE: confirm there is NO "SharedArrayBuffer is not defined" / cross-origin-isolation warning. (Pitfall 5 / numThreads=1)
    4. Confirm the onnxruntime-web version in the loaded `.wasm` CDN URL matches the output of `npm run check:ort-version` locally. (Pitfall 3)
    5. Wait for the model to load (download progress indicator appears then clears). Observe articles: confirm the list renders immediately and only progressively hides items (no blank/blocked screen).
    6. To run the score-validation pass, open the LOCAL dev server (`npm run dev`, which logs scores) and review spam scores for 20+ real articles. Confirm legitimate Bitcoin/Lightning/Nostr articles are NOT scoring as spam at the default threshold (~0.90). An article titled like "Running a Lightning node" scoring > 0.85 is a false-positive warning sign (Pitfall 1).
    7. FAIL-OPEN: in DevTools, block the HF model/CDN request and reload — confirm ALL articles stay visible and the amber "content filter unavailable" notice appears (criterion 4, SPAM-04).
  </how-to-verify>
  <verify>
    <human-check>Report: (a) any .wasm 404s? (b) any SharedArrayBuffer warning? (c) does the wasmPaths version match check:ort-version? (d) for the 20+ articles, are legitimate crypto/Nostr articles over-filtered at ~0.90 — paste the score range you observed; (e) does fail-open keep articles visible with the notice when the model is blocked?</human-check>
  </verify>
  <resume-signal>Reply with the five observations above, then state the GO/NO-GO: "GO — pin threshold to X.XX" (trust spam ML) OR "NO-GO — fall back to language-only + length".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Pin the final threshold OR apply the language-only fallback (go/no-go, D-01)</name>
  <files>src/hooks/useClassification.ts</files>
  <read_first>
    - src/hooks/useClassification.ts (the file being modified — SPAM_THRESHOLD constant + the worker-dispatch branch)
    - .planning/phases/05-ml-pipeline-infrastructure/05-CONTEXT.md (D-01 fallback path, D-02 slider default; D-03 language gate stays regardless)
    - .planning/research/PITFALLS.md (Pitfall 1 recovery: bump threshold; or skip spam ML)
  </read_first>
  <action>
    Apply the Task 2 verdict. IF GO: set `SPAM_THRESHOLD` (and the slider default in App.tsx state if validation pinned a different value than 0.90) to the value the human pinned, with a comment recording the validation date and observed score range. IF NO-GO (legitimate articles over-filter even near 0.90): implement the language-only + length fallback for this milestone — stop dispatching to the ONNX worker (guard the spam dispatch behind a disabled flag so the spam branch never sets 'spam'; keep the worker code in place but bypassed), keep the franc language gate (D-03) and the 500-word length gate (LEN-01/D-06) fully active, and hide or disable the slider since spam ML is deferred (note SPAM-05 deferral in a comment). Either way the franc + length gates remain unchanged. Record the decision and rationale in the SUMMARY. Re-deploy after the change and re-confirm the build is green.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && grep -qE "SPAM_THRESHOLD" src/hooks/useClassification.ts && npm run build 2>&1 | tail -2 && npm test 2>&1 | grep -qiE "(pass|✓)" && echo VERDICT_APPLIED_OK</automated>
  </verify>
  <acceptance_criteria>
    - GO path: SPAM_THRESHOLD set to the human-pinned value (between 0.50 and 0.99) with a validation-date comment; slider default aligned
    - NO-GO path: ONNX spam dispatch disabled (no article ever labelled 'spam'); franc language gate and 500-word length gate still active; SPAM-05 deferral noted in a comment
    - The franc + length gates are unchanged in both paths
    - The SUMMARY records the verdict, observed score range, and final threshold (or fallback)
    - `npm run build` exits 0 and `npm test` passes
    - Re-deploy completed (Actions green) after the verdict change
  </acceptance_criteria>
  <done>The evidence-based go/no-go is applied — either the spam filter is trusted at a pinned threshold or it is bypassed in favor of language-only + length — the build and tests are green, and the result is redeployed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF/jsDelivr CDN → deployed worker | Live deployment depends on third-party CDN availability for model + WASM |
| dev console logging → developer | Scores logged for validation must not leak article content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-DEVLOG | Information Disclosure | dev-only score logging | mitigate | Guard with import.meta.env.DEV (excluded from prod bundle); log only id + score + label, never article body |
| T-05-LIVEFAIL | Availability | model load on live URL | mitigate | Smoke-test confirms fail-open: blocking the CDN leaves all articles visible with the notice (SPAM-04, criterion 4) |
| T-05-OVERFILTER | Tampering | spam threshold vs real corpus | mitigate | D-01 go/no-go gate: trust the filter only after eyeballing 20+ real scores; fall back to language-only + length if legitimate articles over-filter (the blocking human checkpoint cannot be auto-approved) |
| T-05-WASM404 | Availability | wasmPaths on /soveng/ sub-path | mitigate | Live network-tab check for .wasm 404s + version-pin match (Pitfall 2/3) — caught on the real host, not localhost |
</threat_model>

<verification>
- Live URL: no .wasm 404s, no SharedArrayBuffer warning, wasmPaths version matches check:ort-version (human-verified)
- 20+ real article scores reviewed; legitimate crypto/Nostr articles not over-filtered at the pinned threshold (human-verified)
- Fail-open confirmed on the live URL when the model is blocked (human-verified)
- `npm run build` + `npm test` green after the verdict is applied; redeploy Actions green
</verification>

<success_criteria>
All four deployment constraints are verified on the real /soveng/ URL, the spam go/no-go is decided from real scores (threshold pinned or language-only fallback applied), fail-open is confirmed live, and the final build is deployed and recorded.
</success_criteria>

<output>
Create `.planning/phases/05-ml-pipeline-infrastructure/05-06-SUMMARY.md` when done — RECORD the go/no-go verdict, the observed score range, and the final pinned SPAM_THRESHOLD (or the language-only fallback decision).
</output>
