---
phase: 05-ml-pipeline-infrastructure
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/ui/switch.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/badge.tsx
  - src/components/ContentFilterControls.tsx
autonomous: true
requirements: [CTRL-02, CTRL-03, CTRL-04, CTRL-05]
must_haves:
  truths:
    - "A toggle (Switch) turns content filtering on/off (CTRL-04)"
    - "A slider adjusts the spam confidence threshold over 0.50-0.99 (CTRL-05, D-02)"
    - "A download progress indicator appears while the model loads and clears when done (CTRL-02)"
    - "A count of hidden articles is shown while filtering is active (CTRL-03)"
    - "A non-blocking 'content filter unavailable' notice appears on model failure"
    - "All controls use existing shadcn components only and the terminal aesthetic"
  artifacts:
    - path: "src/components/ContentFilterControls.tsx"
      provides: "Presentational controls component with typed props"
      exports: ["ContentFilterControls"]
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch primitive"
    - path: "src/components/ui/slider.tsx"
      provides: "shadcn Slider primitive"
    - path: "src/components/ui/progress.tsx"
      provides: "shadcn Progress primitive"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge primitive"
  key_links:
    - from: "src/components/ContentFilterControls.tsx"
      to: "src/components/ui/slider.tsx"
      via: "Slider import for threshold control"
      pattern: "from ['\"]@/components/ui/slider['\"]"
---

<objective>
Build the user-facing filter controls as a single presentational `ContentFilterControls` component plus the
four shadcn primitives it needs (Switch, Slider, Progress, Badge), styled on the terminal aesthetic. This
component is pure UI: it receives state and callbacks via props and renders the on/off toggle (CTRL-04), the
spam-confidence slider over the 0.50-0.99 range (CTRL-05, D-02), the model-download progress bar (CTRL-02),
the hidden-articles count badge (CTRL-03), and the non-blocking model-failure notice. It owns no business
logic — Plan 05 wires it into App.tsx against the Plan 03 hook contract.

Per D-07: there is NO separate "show hidden"/peek control — the slider at its maximum is the false-positive
recovery mechanism. Per D-09: NO per-article spam/not-spam flagging UI. Per CLAUDE.md: use existing shadcn
components only — do NOT hand-roll new UI primitives.

Purpose: A self-contained, prop-driven controls component that Plan 05 can drop in alongside FilterBar.
Output: four new `src/components/ui/*.tsx` primitives + `src/components/ContentFilterControls.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ml-pipeline-infrastructure/05-CONTEXT.md
@.planning/phases/05-ml-pipeline-infrastructure/05-PATTERNS.md
@src/components/FilterBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the four shadcn primitives (switch, slider, progress, badge)</name>
  <files>src/components/ui/switch.tsx, src/components/ui/slider.tsx, src/components/ui/progress.tsx, src/components/ui/badge.tsx</files>
  <read_first>
    - src/components/ui/checkbox.tsx (an existing shadcn primitive — confirms the project's shadcn conventions: radix-ui unified package, cn util, new-york style)
    - src/components/ui/toggle-group.tsx (another installed primitive for style reference)
    - .planning/phases/05-ml-pipeline-infrastructure/05-PATTERNS.md (the "shadcn Components to Install" section)
    - CLAUDE.md (use existing shadcn components; new-york style, Tailwind v4, React 19; radix consolidated package per 03-01)
  </read_first>
  <action>
    Add the four shadcn components via the project's CLI convention: `npx shadcn@latest add switch slider progress badge`. These scaffold into `src/components/ui/`. Do NOT hand-author the primitives — let the CLI generate them so they match the installed new-york / Tailwind-v4 / radix-consolidated setup used by the existing checkbox and toggle-group components. If the CLI prompts, accept defaults that match the existing config; do not re-init or overwrite components.json. Confirm radix peer deps resolve (the project already uses the consolidated radix-ui package per the 03-01 decision). Do not modify the generated primitives' internals — terminal styling is applied at the consumer via className, exactly how FilterBar styles Checkbox and ToggleGroup.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && test -f src/components/ui/switch.tsx && test -f src/components/ui/slider.tsx && test -f src/components/ui/progress.tsx && test -f src/components/ui/badge.tsx && echo SHADCN_FILES_OK</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under src/components/ui/
    - Each imports from the cn util (`@/lib/utils`) consistent with the existing checkbox.tsx
    - `npx tsc --noEmit` reports no errors in the four new ui files
    - components.json was not re-initialized and existing primitives are unchanged
  </acceptance_criteria>
  <done>Switch, Slider, Progress, and Badge primitives are present in src/components/ui/, generated by the shadcn CLI to match the project's existing component conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ContentFilterControls presentational component</name>
  <files>src/components/ContentFilterControls.tsx</files>
  <read_first>
    - src/components/FilterBar.tsx (the structural analog — typed props interface, shadcn-only, terminal Tailwind tokens, sticky terminal chrome)
    - src/components/ui/switch.tsx, src/components/ui/slider.tsx, src/components/ui/progress.tsx, src/components/ui/badge.tsx (the primitives just added — their prop signatures)
    - .planning/phases/05-ml-pipeline-infrastructure/05-PATTERNS.md (the ContentFilterControls.tsx sketch — props interface, slider 50-99 mapping, terminal classes, four rows)
    - .planning/phases/05-ml-pipeline-infrastructure/05-CONTEXT.md (D-02 slider range/default, D-07 slider-is-recovery, D-08 controls list, D-09 no flagging)
  </read_first>
  <action>
    Create `src/components/ContentFilterControls.tsx` as a presentational component mirroring FilterBar's structure. Typed props interface: `filterEnabled: boolean`, `onFilterEnabledChange: (enabled: boolean) => void`, `spamThreshold: number` (0-1 float), `onSpamThresholdChange: (threshold: number) => void`, `filteredCount: number`, `downloadProgress: number | null`, `modelFailed: boolean`. Render four rows using only the shadcn primitives + terminal tokens (font-mono text-xs, text-terminal-green-dim/muted/amber, border-terminal-border, rounded-none, data-[state]:bg-terminal-* styling, matching FilterBar): (1) Switch toggle labelled "content filter" (CTRL-04) plus a Badge "{filteredCount} hidden" shown only when filterEnabled and filteredCount > 0 (CTRL-03); (2) Slider for the spam gate shown only when filterEnabled — map the 0-1 float to an integer 50-99 (min 50, max 99, step 1) and back via /100, displaying the percentage (CTRL-05, D-02 range 0.50-0.99 default ~0.90); add a small caption noting the max disables the spam filter (D-07 — the slider IS the recovery, no separate peek control); (3) a Progress bar + percentage shown only when downloadProgress is not null (CTRL-02); (4) a non-blocking amber "[WARN] content filter unavailable — all articles shown" notice shown only when modelFailed. Do NOT add any per-article spam flagging or feedback UI (D-09). Keep it stateless — all state arrives via props.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/nostr/soveng && grep -q "export function ContentFilterControls" src/components/ContentFilterControls.tsx && grep -q "@/components/ui/slider" src/components/ContentFilterControls.tsx && grep -q "@/components/ui/switch" src/components/ContentFilterControls.tsx && npm run build 2>&1 | tail -2 && echo CONTROLS_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ContentFilterControls.tsx exports `ContentFilterControls`
    - The props interface contains exactly: filterEnabled, onFilterEnabledChange, spamThreshold, onSpamThresholdChange, filteredCount, downloadProgress, modelFailed
    - Slider min=50 max=99 step=1, value derived from spamThreshold*100, onValueChange maps back via /100 (range 0.50-0.99)
    - Badge renders only when filterEnabled && filteredCount > 0
    - Progress renders only when downloadProgress !== null
    - The modelFailed notice uses text-terminal-amber and is non-blocking (a sibling, not a replacement of the list)
    - Uses only existing shadcn components (Switch/Slider/Progress/Badge) — no new hand-rolled primitives, no per-article flagging
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ContentFilterControls renders the toggle, threshold slider (0.50-0.99), download progress, hidden-count badge, and model-failure notice from props alone, on the terminal aesthetic, using shadcn primitives only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → control callbacks | Slider/Switch values flow to App state; component is presentational and must not itself gate content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-UIRANGE | Tampering | Slider threshold range | mitigate | Slider clamps to min 50 / max 99 so the threshold can never be driven below the conservative floor; the App is the source of truth for the actual filter decision |
| T-05-UITRUST | Information Disclosure | controls component | accept | Purely presentational, no secrets, no per-article disclosure (D-09); renders only counts/progress |
| T-05-SC | Tampering | npx shadcn add (switch/slider/progress/badge) | accept | shadcn CLI scaffolds local component source from the official registry into the repo; no new runtime npm dependency beyond already-present radix-ui consolidated package |
</threat_model>

<verification>
- All four shadcn primitive files exist and typecheck
- `npm run build` exits 0 with ContentFilterControls included
- Slider range is 50-99 (maps to 0.50-0.99); no peek/flagging UI present
</verification>

<success_criteria>
A prop-driven ContentFilterControls component renders all five control surfaces (toggle, slider, progress, count badge, failure notice) using shadcn primitives and terminal tokens, with the slider doubling as the false-positive recovery (D-07) and no per-article flagging (D-09). Build is green.
</success_criteria>

<output>
Create `.planning/phases/05-ml-pipeline-infrastructure/05-04-SUMMARY.md` when done — record the final ContentFilterControlsProps shape for Plan 05 wiring.
</output>
