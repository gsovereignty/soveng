---
phase: 02-nostr-data-layer
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/context/nostrReducer.test.ts
  - src/context/NostrContext.tsx
  - src/hooks/useProfileFetch.ts
  - src/App.tsx
requirements: [DATA-04]
autonomous: true
must_haves:
  truths:
    - "useNostr() exposes a refetch() that resets state and re-opens both subscriptions (D-12)"
    - "refetch() clears seenCoords and articles so previously-seen coordinates can reappear on a fresh fetch (Pitfall 3)"
    - "Re-running the article effect (fetchKey bump) tears down the prior subscription via cleanup before re-subscribing — no leaked WebSocket"
    - "The profile subscription re-fires after refetch once articles arrive again"
    - "App surfaces a retry affordance on error/empty status that calls refetch()"
  artifacts:
    - path: "src/context/NostrContext.tsx"
      provides: "refetch exposed on context value + fetchKey-driven re-subscription"
      contains: "refetch"
  key_links:
    - from: "src/App.tsx"
      to: "src/context/NostrContext.tsx"
      via: "useNostr().refetch on retry control"
      pattern: "refetch"
    - from: "src/hooks/useArticleFetch.ts"
      to: "fetchKey effect dependency"
      via: "useEffect [fetchKey] re-run + cleanup teardown"
      pattern: "fetchKey"
---

<objective>
Complete the data layer with the refetch/retry slice: confirm `RESET` fully clears streaming state (seenCoords, articles, profiles) and bumps `fetchKey`, ensure the article and profile effects re-run cleanly on `refetch()` (cleanup tears down the prior subscription before re-subscribing), expose `refetch()` through `useNostr()`, and surface a terminal-styled retry affordance in `App.tsx` on the `error`/`empty` states.

Purpose: A hung or empty relay round must be recoverable without a page reload (D-12). This closes DATA-04's resilience requirement: not just "a timeout never blocks rendering" but "the user can re-attempt after a timeout/empty/error resolution." It also guards Pitfall 3 (stale seenCoords blocking the second fetch).

Output: `refetch()` exposed and wired to a retry control; re-fetch produces a fresh stream with no leaked subscriptions and no stale-dedup starvation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-nostr-data-layer/02-CONTEXT.md
@.planning/phases/02-nostr-data-layer/02-RESEARCH.md
@.planning/phases/02-nostr-data-layer/02-PATTERNS.md
@.planning/phases/02-nostr-data-layer/02-01-PLAN.md
@.planning/phases/02-nostr-data-layer/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden RESET / refetch re-subscription correctness (reducer test + context wiring)</name>
  <files>src/context/nostrReducer.test.ts, src/context/NostrContext.tsx, src/hooks/useProfileFetch.ts</files>
  <read_first>
    - .planning/phases/02-nostr-data-layer/02-RESEARCH.md (Pattern 6 "Subscription teardown on refetch" — RESET bumps fetchKey, effect cleanup closes sub, re-runs; Pitfall 3 reset seenCoords; Open Question 2 profile re-sub timing)
    - .planning/phases/02-nostr-data-layer/02-CONTEXT.md (D-12 refetch resets + re-opens; D-04 timer cleanup role; D-06 status resets to streaming)
    - .planning/phases/02-nostr-data-layer/02-PATTERNS.md (useEffect cleanup pattern — StrictMode double-mount; refetch via fetchKey)
    - src/context/nostrReducer.ts (RESET already returns fresh initialState + fetchKey+1 from Plan 01 — verify, don't regress)
    - src/context/nostrReducer.test.ts (current — extend with full-reset assertions)
    - src/context/NostrContext.tsx (current — refetch useCallback already dispatches RESET from Plan 01; useArticleFetch keyed on fetchKey; useProfileFetch keyed on pubkeys)
    - src/hooks/useArticleFetch.ts (fetchKey effect dep + cleanup sub.close — verify teardown path)
    - src/hooks/useProfileFetch.ts (pubkeys dep — after RESET, articles []→ pubkeys [] → effect cleanup closes profile sub; re-fires when articles repopulate)
  </read_first>
  <behavior>
    - RESET after a populated state returns: articles === [], seenCoords.size === 0, profiles.size === 0, status === 'streaming', fetchKey === prev + 1.
    - A coordinate present in seenCoords before RESET is NOT in seenCoords after RESET (so the same article can be re-added on the next fetch).
    - After RESET, a subsequent ARTICLE_RECEIVED for a previously-seen coordinate is accepted (not deduped away) — proves Pitfall 3 is closed.
  </behavior>
  <action>
    Extend `src/context/nostrReducer.test.ts`: build a state with several articles + coordinates + a profile, dispatch RESET, assert the full fresh-reset behavior above, then dispatch ARTICLE_RECEIVED for a coordinate that existed pre-reset and assert it IS added (length 1). This is the regression guard for Pitfall 3.
    Verify (and only adjust if missing) `src/context/NostrContext.tsx`: `refetch = useCallback(() => dispatch({ type: 'RESET' }), [])` is present and included in the context `value`; `useArticleFetch(state.fetchKey, dispatch, state.articles.length)` is keyed such that the article effect's `[fetchKey]` dependency re-runs on RESET (React runs the effect cleanup — `sub.close('effect cleanup')` — before re-subscribing, the correct idiom; no manual prev-sub tracking).
    Verify `src/hooks/useProfileFetch.ts`: because RESET empties `state.articles`, the derived `pubkeys` becomes `[]`, the profile effect cleanup closes the prior profile subscription, and when fresh articles arrive the effect re-fires with the new pubkey set. No code change should be required here beyond confirming the dependency wiring; adjust only if the teardown path is broken (e.g. add the missing early-return cleanup).
    Do NOT introduce manual subscription bookkeeping across renders — rely on the effect-dependency + cleanup idiom (RESEARCH Pattern 6).
  </action>
  <verify>
    <automated>npx vitest run src/context/nostrReducer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/context/nostrReducer.test.ts` exits 0; full-reset test and "previously-seen coordinate re-added after RESET" test both pass
    - RESET test asserts seenCoords.size === 0 AND profiles.size === 0 AND status === 'streaming' AND fetchKey incremented
    - `NostrContext.tsx` exposes `refetch` in the context value (grep: `refetch` in the value object)
    - article effect is keyed on `[fetchKey]` and its cleanup calls `sub.close` (source: cleanup return present in useArticleFetch)
    - profile effect closes its subscription in cleanup so an empty pubkey set after RESET tears it down
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>RESET fully clears streaming state (seenCoords/articles/profiles) and re-arms a fresh fetch; refetch() re-runs both effects with clean teardown and no stale-dedup starvation — unit-tested against Pitfall 3.</done>
</task>

<task type="auto">
  <name>Task 2: Surface a terminal-styled retry affordance on error/empty status in App</name>
  <files>src/App.tsx</files>
  <read_first>
    - .planning/phases/02-nostr-data-layer/02-PATTERNS.md (App.tsx MODIFY — preserve terminal tokens + default export; Terminal Theme Token Usage; "use existing shadcn components" — Button is available if added, else terminal-styled element)
    - .planning/phases/02-nostr-data-layer/02-CONTEXT.md (D-06 status surface; D-12 refetch wired to retry; specifics — BootSequence backs streaming; deferred — no per-relay UI)
    - CLAUDE.md (use EXISTING shadcn components; do NOT create new UI components; terminal aesthetic)
    - src/App.tsx (current after Plan 01 — AppShell reads useNostr(); streaming → BootSequence; placeholder for other statuses)
    - src/components/ui/card.tsx (existing shadcn Card available for the message surface)
    - src/context/NostrContext.tsx (useNostr exposes status + refetch)
  </read_first>
  <action>
    Modify `src/App.tsx` (the inner shell that calls `useNostr()`). For `status === 'error'` and `status === 'empty'`, render a terminal-styled message plus a retry control that calls `refetch()` from `useNostr()`. Use ONLY existing components: if a shadcn `Button` already exists under `src/components/ui/`, use it; otherwise use a plain `<button>` styled with existing terminal tokens (`text-terminal-green`, `border-terminal-border`, `crt-glow`, `font-mono`) — do NOT scaffold a new shadcn component in this phase unless `npx shadcn@latest add button` is the chosen path, in which case add only the stock `button` component (no custom component authored). Message copy distinguishes the two states: error = "relay connection failed" (all relays errored), empty = "no articles found" (relays responded, zero results) — keeping D-06's error≠empty distinction visible. The retry control's onClick calls `refetch()`. Preserve `export default`, the header/footer, and all terminal class tokens. For `status === 'done'` keep the minimal Phase 2 placeholder (article count / `<pre>`); Phase 3 renders the real list. `streaming` continues to render `<BootSequence />`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `App.tsx` renders distinct copy for `error` vs `empty` (grep both branches; messages differ)
    - a retry control's handler calls `refetch()` (grep: `refetch` invoked in an onClick/handler)
    - retry uses an existing component or terminal-token-styled `<button>` — NO new bespoke UI component file authored under `src/components/` (only `src/components/ui/button.tsx` permitted IF added via shadcn CLI)
    - `export default` preserved; terminal tokens retained (grep: `bg-terminal-bg`, `crt-glow`)
    - `BootSequence` still rendered for `status === 'streaming'`
  </acceptance_criteria>
  <done>error and empty states show distinct terminal-styled messages with a working retry control wired to refetch(); streaming still backed by BootSequence; no bespoke UI components introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Nostr relays → data layer (refetch path) | A second fetch round crosses the same untrusted-relay boundary; refetch must not leak prior-round subscriptions or accumulate unbounded WebSocket connections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | DoS / Resource leak | refetch leaks prior subscriptions / accumulates WebSockets | mitigate | refetch bumps `fetchKey`; React runs the article effect cleanup (`sub.close`) before re-subscribing; profile effect tears down when pubkeys empties on RESET. No manual cross-render sub bookkeeping. StrictMode double-mount also exercised by cleanup. |
| T-02-12 | DoS | Stale seenCoords starves the second fetch (0 articles forever) | mitigate | RESET returns fresh initialState (empty seenCoords) — unit-tested that a previously-seen coordinate is re-added after RESET (Pitfall 3 regression guard). |
| T-02-13 | Tampering | Retry control re-renders untrusted content | accept | Phase 2 renders only status text + counts; no relay-supplied strings rendered as HTML. Retry messages are static literals. XSS surface remains a Phase 4 render concern. |

## Downstream (Phase 3) assumptions flagged
- Phase 3 will replace the Phase 2 `done`-state placeholder with the real article list but should reuse the SAME `refetch()` + error/empty distinction wired here (DISP-05).
</threat_model>

<verification>
- `npx vitest run` passes (full-reset + Pitfall-3 regression tests).
- `npm run build` exits 0.
- Manual (executor): `npm run dev`; force an empty/error round if possible (e.g. temporarily break relay URLs in a local edit) and confirm the retry control re-opens the stream and clears stale state; revert the edit.
</verification>

<success_criteria>
- DATA-04: refetch() recovers from timeout/empty/error rounds; a hung relay never permanently blocks — the user can re-attempt.
- D-12: refetch() exposed via useNostr() and wired to a retry affordance.
- Pitfall 3 closed: fresh fetch is not starved by stale seenCoords.
- error≠empty distinction visible in the retry UI per D-06.
</success_criteria>

<output>
Create `.planning/phases/02-nostr-data-layer/02-03-SUMMARY.md` when done.
</output>
